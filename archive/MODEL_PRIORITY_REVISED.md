# 新モデル実装優先順位（修正版）

## ❌ 天候アダプティブモデルの問題点

### データの時間差問題

```
【現状のデータフロー】
深夜3時: スクレイピング実行
  ↓
  天候データ取得（風速0m、曇り）
  ↓
  AI予想生成
  ↓
午前10時: レース開始
  ↓
  実際の天候: 風速5m、晴れ ← データと異なる！
```

**結論:**
- ❌ 現在の天候データは7時間も前のもの
- ❌ 風向・風速は刻々と変化
- ❌ 予想精度が下がる可能性が高い
- → **天候アダプティブモデルは実装不可**

---

## ✅ 実装可能なモデル（データの時間差の影響なし）

### 1. 選手実力ベースのモデル（時間に依存しない）

#### 📊 実力格差モデル
**コンセプト:** 選手間の実力差が大きいレースを狙う

**データ依存性:**
- 全国勝率 → ✅ 変化しない（選手の過去成績）
- 級別 → ✅ 変化しない
- 年齢 → ✅ 変化しない

**ロジック:**
```javascript
function calculatePowerGapScore(racer, index, allRacers) {
  // 全選手の勝率の標準偏差を計算
  const winRates = allRacers.map(r => r.globalWinRate);
  const avgRate = winRates.reduce((sum, r) => sum + r, 0) / 6;
  const stdDev = Math.sqrt(
    winRates.reduce((sum, r) => sum + Math.pow(r - avgRate, 2), 0) / 6
  );

  let score = racer.globalWinRate * 120;

  // 実力差が大きい（stdDev > 1.5）レースでトップの選手を高評価
  if (stdDev > 1.5 && racer.globalWinRate > avgRate + stdDev) {
    score += 300; // 圧倒的実力者ボーナス
  }

  // 実力が拮抗している（stdDev < 1.0）レースでは機材重視
  if (stdDev < 1.0) {
    score += racer.motor2Rate * 150;
    score += racer.boat2Rate * 80;
  }

  score += racer.local2Rate * 50;
  score -= index * 10;

  return Math.floor(score);
}
```

**特徴:**
- ✅ 時間に依存しない
- ✅ 「格上選手が格下を圧倒」するレースを狙う
- ✅ 実力拮抗レースでは機材性能を重視

---

#### 🏆 ベテラン安定モデル
**コンセプト:** 経験豊富なA級選手を重視

**ロジック:**
```javascript
function calculateVeteranScore(racer, index) {
  let score = 0;

  // A1級の経験者に大ボーナス
  if (racer.grade === 'A1' && racer.age >= 35) {
    score += 500; // ベテランA1級
  } else if (racer.grade === 'A1') {
    score += 350; // 若手A1級
  } else if (racer.grade === 'A2' && racer.age >= 40) {
    score += 250; // ベテランA2級
  }

  // 勝率重視
  score += racer.globalWinRate * 150;
  score += racer.global2Rate * 80;

  // 当地経験
  if (racer.localWinRate > 0) {
    score += racer.localWinRate * 100;
  }

  // モーター性能
  score += racer.motor2Rate * 50;

  // 枠番補正
  score -= index * 8;

  return Math.floor(score);
}
```

**特徴:**
- ✅ A1級ベテランの安定感を評価
- ✅ 的中率重視（回収率は控えめ）
- ✅ 初心者ユーザー向け

---

### 2. 機材性能特化モデル（時間に依存しない）

#### 🔧 モーター総合評価モデル
**コンセプト:** モーター・ボート両方の性能を総合評価

**ロジック:**
```javascript
function calculateEquipmentScore(racer, index, allRacers) {
  // 全選手のモーター平均を計算
  const avgMotor = allRacers.reduce((sum, r) => sum + r.motor2Rate, 0) / 6;
  const avgBoat = allRacers.reduce((sum, r) => sum + r.boat2Rate, 0) / 6;

  // 平均との差分
  const motorGap = racer.motor2Rate - avgMotor;
  const boatGap = racer.boat2Rate - avgBoat;

  let score = 0;

  // モーター性能を最重視
  score += racer.motor2Rate * 200;

  // 平均以上なら追加ボーナス
  if (motorGap > 0) {
    score += motorGap * 100;
  }

  // ボート性能
  score += racer.boat2Rate * 120;
  if (boatGap > 0) {
    score += boatGap * 60;
  }

  // 選手の腕（モーター調整力）
  score += racer.globalWinRate * 60;

  // モーター+ボート両方が平均以上なら大ボーナス
  if (motorGap > 5 && boatGap > 5) {
    score += 300;
  }

  return Math.floor(score);
}
```

**特徴:**
- ✅ 機材性能の総合評価
- ✅ 「良いモーター×良いボート」の相乗効果
- ✅ モーター調整が上手い選手を評価

---

### 3. 当地特化モデル（地元の鬼・改良版）

#### 🎯 地元アドバンテージモデル
**コンセプト:** 当地成績が全国成績を大きく上回る選手を狙う

**ロジック:**
```javascript
function calculateLocalAdvantageScore(racer, index) {
  const localAdvantage = racer.localWinRate - racer.globalWinRate;

  let score = 0;

  // 当地アドバンテージが大きいほど高評価
  if (localAdvantage > 2.0) {
    score += 600; // 当地で圧倒的
  } else if (localAdvantage > 1.0) {
    score += 400; // 当地で有利
  } else if (localAdvantage > 0) {
    score += 200; // やや有利
  } else if (localAdvantage < -1.0) {
    score -= 300; // 当地苦手
  }

  // 当地成績を最重視
  score += racer.localWinRate * 250;
  score += racer.local2Rate * 150;

  // 当地未経験は大幅減点
  if (racer.localWinRate === 0 && racer.local2Rate === 0) {
    score -= 400;
  }

  // 機材性能
  score += racer.motor2Rate * 80;

  // 全国勝率は参考程度
  score += racer.globalWinRate * 40;

  return Math.floor(score);
}
```

**特徴:**
- ✅ 「地元の利」を最大限活用
- ✅ 場の特性を熟知した選手を評価
- ✅ 当地未経験者を避ける

---

## 🎯 修正後の実装優先順位

### 第1優先：選手実力ベース（今すぐ実装可能）

| モデル | 実装難易度 | 期待効果 | データ依存性 |
|--------|----------|---------|------------|
| 実力格差モデル | 低 | 中 | ✅ なし |
| ベテラン安定モデル | 低 | 中 | ✅ なし |
| 地元アドバンテージ | 低 | 中 | ✅ なし |
| モーター総合評価 | 低 | 中 | ✅ なし |

**実装時間:** 合計2-3時間

---

### 第2優先：直前情報の取得（最重要インフラ）

**追加スクレイピング:**
```javascript
// レース開始30分前に直前情報を取得
// scripts/scrape-beforeinfo.js（新規作成）

async function getBeforeInfo(date, placeCd, raceNo) {
  const url = `https://www.boatrace.jp/owpc/pc/race/beforeinfo?rno=${raceNo}&jcd=${placeCd}&hd=${date}`;

  // 取得データ:
  // - 展示タイム（最重要）
  // - ST（スタートタイミング）
  // - チルト角度
  // - 体重
  // - 部品交換情報
  // - リアルタイム天候（この時点で正確）
}
```

**GitHub Actions修正案:**
```yaml
# レース開始30分前に実行（例: 10:15, 11:15, ...）
- cron: '15 1-8 * * *'  # JST 10:15-17:15
```

**実装時間:** 1-2日

---

### 第3優先：直前情報活用モデル（最高精度）

直前情報取得後に実装:

| モデル | 必要データ | 期待効果 |
|--------|----------|---------|
| 展示タイムハンター | 展示タイム、ST | ⭐⭐⭐ 非常に高い |
| 天候アダプティブ | リアルタイム天候 | ⭐⭐ 高い |
| 進入隊形予測 | 展示進入コース | ⭐⭐ 高い |

---

## 📝 修正後の実装ロードマップ

### Week 1: 選手実力ベースモデル（4つ）
- [x] データ検証完了
- [ ] 実力格差モデル実装
- [ ] ベテラン安定モデル実装
- [ ] 地元アドバンテージモデル実装
- [ ] モーター総合評価モデル実装
- [ ] 4モデルをA/Bテスト開始

**期待される成果:**
- 現在の3モデル → 7モデルに拡充
- ユーザーの選択肢が増える
- 30日後に最良モデルを特定

---

### Week 2-3: 直前情報スクレイピング
- [ ] beforeinfoページのスクレイピング実装
- [ ] GitHub Actionsのスケジュール変更
  - レース開始30分前に実行
  - 展示タイム・ST・リアルタイム天候を取得
- [ ] データ構造の拡張

---

### Week 4-: 直前情報活用モデル
- [ ] 展示タイムハンターモデル
- [ ] 天候アダプティブモデル（リアルタイム版）
- [ ] 進入隊形予測モデル

---

## ⚠️ 重要な学び

### データの時間差を常に意識する

```
【悪い例】
❌ 深夜のデータで午前のレースを予想
❌ 変動するデータ（天候）に依存
❌ 時間差を考慮しない

【良い例】
✅ 選手の過去成績（変化しない）
✅ 機材データ（レース当日は固定）
✅ 直前情報を取得（レース30分前）
```

### モデル設計の原則

1. **データの性質を理解する**
   - 静的データ: 選手成績、級別、年齢
   - 動的データ: 天候、展示タイム、オッズ

2. **取得タイミングを考慮する**
   - 現在: 深夜〜早朝（レースの数時間前）
   - 必要: レース直前（30分前）

3. **時間差の影響を評価する**
   - 影響なし: 選手実力、機材
   - 影響大: 天候、展示タイム

---

## 🎯 即座に実装すべきモデル

ユーザーの指摘を受けて、優先順位を変更します：

### ✅ 今日から実装可能（データの時間差なし）

1. **実力格差モデル** - 30分で実装
2. **モーター総合評価モデル** - 30分で実装
3. **地元アドバンテージモデル** - 30分で実装
4. **ベテラン安定モデル** - 30分で実装

### ⏳ 直前情報取得後に実装

5. **展示タイムハンター** - 最高精度（最優先）
6. **天候アダプティブ** - リアルタイム版
7. **進入隊形予測**

---

## 📊 期待される成果

### 短期（1週間後）
- 7モデル体制（現在3 → 新規4追加）
- ユーザーの選択肢拡大
- A/Bテストで最良モデル特定

### 中期（1ヶ月後）
- 直前情報スクレイピング稼働
- 展示タイムハンター投入
- 予想精度が大幅向上

### 長期（3ヶ月後）
- 10モデル以上の運用
- 会場別・条件別の推奨モデル自動選択
- 回収率150%超えを目指す
